# booking-management-service
A backend system for booking management service built using Java and Spring Boot. It handles core workflows like property listing, search, booking, and payment while ensuring data consistency and preventing double booking through transactional control and validation.
